const Header = () => {
  return (
    <header className="header">
      <img className='logo' src={require('./../logo.png')} />
      <div>ава</div>
      <img className='cart' src={require('./../cart.png')} />
    </header>
  );
};

export default Header;
